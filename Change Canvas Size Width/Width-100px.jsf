	if (fw.documents.length == 0) {
		alert("Please open a file.");
	}else {
var dom = fw.getDocumentDOM();
var value=-100;
dom.setDocumentCanvasSize({left:0, top:0, right:(dom.width+value), bottom:dom.height}, true);
}